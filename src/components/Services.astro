---
import config from '../config.json';

const icons = {
  scissors: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M20 4L8.5 15.5M14.5 4L4 14.5"/><circle cx="6" cy="6" r="2"/><circle cx="6" cy="18" r="2"/><circle cx="20" cy="4" r="2"/></svg>`,
  razor: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2v20M2 12h20M17 7l-10 10M17 17L7 7"/></svg>`,
  shave: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="4" y="4" width="16" height="16" rx="2"/><path d="M8 2v4M16 2v4M8 20v2M16 20v2"/></svg>`
};
---

<section id="services" class="services-section">
  <div class="services-container">
    <div class="services-header">
      <span class="section-label">Nuestros Servicios</span>
      <h2 class="section-title">
        Maestría en <br/>
        <span class="title-accent">cada detalle</span>
      </h2>
      <p class="section-description">
        Servicios diseñados para el caballero moderno que valora la calidad y la atención al detalle.
      </p>
    </div>

    <div class="services-grid">
      {config.services.map((service, index) => (
        <article class="service-card" style={`animation-delay: ${index * 0.1}s`}>
          <div class="card-border"></div>
          <div class="card-glow"></div>

          <div class="service-icon" set:html={icons[service.icon]} />

          <div class="service-content">
            <h3 class="service-name">{service.name}</h3>
            <p class="service-description">{service.description}</p>
          </div>

          <div class="service-footer">
            <span class="service-price">
              <span class="currency">$</span>
              <span class="amount">{service.price}</span>
              <span class="period">MXN</span>
            </span>
            <a href={`https://wa.me/${config.business.whatsapp}?text=Hola, me interesa el servicio de ${service.name}`}
               class="service-link"
               target="_blank"
               rel="noopener"
               aria-label={`Agendar ${service.name}`}>
              <span>Agendar</span>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M5 12h14M12 5l7 7-7 7"/>
              </svg>
            </a>
          </div>
        </article>
      ))}
    </div>

    <div class="services-note">
      <div class="note-icon">✦</div>
      <p>Todos los servicios incluyen consulta personalizada y productos premium</p>
    </div>
  </div>
</section>

<style>
.services-section {
  position: relative;
  padding: 8rem 2rem;
  background: #0a0a0a;
  overflow: hidden;
}

.services-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 1px;
  height: 100px;
  background: linear-gradient(180deg, transparent, #d4af37, transparent);
}

.services-container {
  max-width: 1200px;
  margin: 0 auto;
}

.services-header {
  text-align: center;
  margin-bottom: 5rem;
}

.section-label {
  display: inline-block;
  font-size: 0.875rem;
  letter-spacing: 0.2em;
  color: #d4af37;
  text-transform: uppercase;
  margin-bottom: 1rem;
  position: relative;
  padding: 0 2rem;
}

.section-label::before,
.section-label::after {
  content: '';
  position: absolute;
  top: 50%;
  width: 20px;
  height: 1px;
  background: #d4af37;
}

.section-label::before {
  left: 0;
}

.section-label::after {
  right: 0;
}

.section-title {
  font-family: 'Playfair Display', serif;
  font-size: clamp(2.5rem, 6vw, 4rem);
  font-weight: 700;
  line-height: 1.2;
  color: #ffffff;
  margin-bottom: 1.5rem;
}

.title-accent {
  background: linear-gradient(135deg, #d4af37 0%, #f4e5c3 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.section-description {
  font-size: 1.125rem;
  color: #a8a8a8;
  max-width: 600px;
  margin: 0 auto;
  line-height: 1.6;
}

.services-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
  margin-bottom: 4rem;
}

.service-card {
  position: relative;
  background: linear-gradient(135deg, #141414 0%, #0f0f0f 100%);
  border: 1px solid rgba(212, 175, 55, 0.1);
  padding: 2.5rem;
  border-radius: 4px;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  opacity: 0;
  animation: cardFadeIn 0.6s ease-out forwards;
}

@keyframes cardFadeIn {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.card-border {
  position: absolute;
  inset: 0;
  border-radius: 4px;
  padding: 1px;
  background: linear-gradient(135deg, #d4af37, transparent, #d4af37);
  -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
  opacity: 0;
  transition: opacity 0.4s;
}

.card-glow {
  position: absolute;
  inset: -100px;
  background: radial-gradient(circle at center, rgba(212, 175, 55, 0.15), transparent 70%);
  opacity: 0;
  transition: opacity 0.4s;
}

.service-card:hover {
  transform: translateY(-8px);
  border-color: rgba(212, 175, 55, 0.3);
  box-shadow: 0 20px 60px rgba(212, 175, 55, 0.2);
}

.service-card:hover .card-border,
.service-card:hover .card-glow {
  opacity: 1;
}

.service-icon {
  width: 48px;
  height: 48px;
  color: #d4af37;
  margin-bottom: 2rem;
  transition: transform 0.4s;
}

.service-card:hover .service-icon {
  transform: scale(1.1) rotate(5deg);
}

.service-content {
  margin-bottom: 2rem;
}

.service-name {
  font-family: 'Playfair Display', serif;
  font-size: 1.75rem;
  font-weight: 700;
  color: #ffffff;
  margin-bottom: 0.75rem;
}

.service-description {
  font-size: 1rem;
  color: #a8a8a8;
  line-height: 1.6;
}

.service-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-top: 2rem;
  border-top: 1px solid rgba(212, 175, 55, 0.1);
}

.service-price {
  display: flex;
  align-items: baseline;
  gap: 0.25rem;
  color: #d4af37;
}

.currency {
  font-size: 1rem;
  font-weight: 500;
}

.amount {
  font-size: 2rem;
  font-weight: 700;
  font-family: 'Playfair Display', serif;
}

.period {
  font-size: 0.875rem;
  color: #8a8a8a;
}

.service-link {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1.5rem;
  background: rgba(212, 175, 55, 0.1);
  color: #d4af37;
  text-decoration: none;
  border: 1px solid rgba(212, 175, 55, 0.2);
  border-radius: 2px;
  font-size: 0.875rem;
  font-weight: 500;
  transition: all 0.3s;
}

.service-link:hover {
  background: rgba(212, 175, 55, 0.2);
  border-color: rgba(212, 175, 55, 0.4);
  transform: translateX(4px);
}

.services-note {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 1rem;
  padding: 2rem;
  background: linear-gradient(135deg, rgba(212, 175, 55, 0.05), rgba(212, 175, 55, 0.02));
  border: 1px solid rgba(212, 175, 55, 0.1);
  border-radius: 4px;
  text-align: center;
}

.note-icon {
  color: #d4af37;
  font-size: 1.25rem;
}

.services-note p {
  font-size: 0.875rem;
  color: #a8a8a8;
  font-style: italic;
}

@media (max-width: 768px) {
  .services-section {
    padding: 5rem 1.5rem;
  }

  .services-grid {
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  .service-footer {
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
  }

  .service-link {
    width: 100%;
    justify-content: center;
  }
}
</style>
